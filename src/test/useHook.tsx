import React, { useState } from "react";


// use use hook to read value: promise or context
// currently not available now
const useHook = () => {

    return <>
        <h1>useHook</h1>
    </>;
}

export default useHook;